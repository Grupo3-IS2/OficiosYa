# The code Lombok generates (getters, builders, equals...) is marked as generated, so JaCoCo leaves it
# out of the coverage: what is measured is the code that was written by hand.
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
